<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} | FinanceFlux</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <nav>
            <div class="logo">
                <a href="/">FinanceFlux</a>
            </div>
            <div class="nav-links">
                <a href="/">Home</a>
                {{#if isAuthenticated}}
                    <a href="/dashboard">Dashboard</a>
                    <div class="user-menu">
                        <span>{{user.email}}</span>
                        <button id="signout-btn">Sign Out</button>
                    </div>
                {{else}}
                    <button id="signin-btn">Sign In</button>
                {{/if}}
            </div>
        </nav>
    </header>

    <main>
        {{{body}}}
    </main>

    {{> auth-modal}}

    <footer>
        <p>&copy; 2025 FinanceFlux. All rights reserved.</p>
    </footer>

    <script src="/js/app.js"></script>
</body>
</html>
